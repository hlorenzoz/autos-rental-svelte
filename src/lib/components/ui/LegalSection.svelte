<script lang="ts">
  import type { Snippet } from 'svelte';

  interface Props {
    title: string;
    icon?: string;
    children?: Snippet;
  }

  let { title, icon, children }: Props = $props();
</script>

<section class="bg-surface-container-lowest border border-outline-variant rounded-lg p-6 lg:p-8 atmospheric-shadow">
  <div class="flex items-start gap-4 mb-4">
    {#if icon}
      <div class="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center text-primary shrink-0">
        <span class="material-symbols-outlined text-md">{icon}</span>
      </div>
    {/if}
    <h2 class="text-h3 font-display font-bold text-on-surface pt-1">
      {title}
    </h2>
  </div>

  <div class="text-body-md text-on-surface-variant leading-relaxed space-y-4">
    {@render children?.()}
  </div>
</section>
